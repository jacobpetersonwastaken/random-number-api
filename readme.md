# Aws lambda random number api
This is a simple api I created that serves random numbers to the endpoint that is set up using Aws API Gateway and Lambda

## Tech Used
- Aws Lambda
- Aws API Gateway

![]()